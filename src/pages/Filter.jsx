import React from 'react';

const Filter = (props) => {

    return (
        <div>
            <h1>Filter</h1>
            <h2>Got anymore them Filters?</h2>
        </div>

    );
};

export default Filter;